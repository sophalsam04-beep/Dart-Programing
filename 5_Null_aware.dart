// Null aware / Null coalescing in flutter

void main(List<String> args) {
      String? name;
      String? department;
      print(name ?? "Guest");
      print(department ?? "No department");

      
}