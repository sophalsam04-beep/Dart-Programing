// Ternary operator
void main(List<String> args) {
      int age = 20;
      String result = age >=19 ? "Sophal" : "Un virak";
      print(result);

      int x = 20;
      String answer = x >=55 ? "yes" : "No";
      print(answer);

      String role = "Leturer IT";
      String show = role = true ? "Yes" : "No";

      print(show);
}